# Backend - Gerenciador de Projetos de Engenharia
# Dependências Python

# Framework Web
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Banco de Dados
# SQLite já vem embutido no Python (não precisa instalar)
# MySQL é opcional - instale apenas se usar DB_TYPE=mysql
# mysql-connector-python==8.2.0

# Autenticação e Segurança
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0

# Validação
pydantic==2.5.0
email-validator==2.1.0

# Rate Limiting
slowapi==0.1.9

# Email (SendGrid)
sendgrid==6.11.0

# Tarefas agendadas (Backup)
schedule==1.2.0

# Cliente HTTP Assíncrono (para integração ChatGPT)
httpx==0.25.2

# Utilitários
python-dateutil==2.8.2

# Health Check (Docker)
requests==2.31.0
