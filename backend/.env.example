# ====================================================
# 🔒 CONFIGURAÇÕES SEGURAS - COPY PARA .env
# ====================================================
# NÃO COMMITAR ARQUIVO .env NO GIT
# Adicionar .env ao .gitignore

# -------- DATABASE --------
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=sua_senha_mysql_aqui
DB_NAME=gerenciador_projetos

# -------- SEGURANÇA JWT --------
# 🔑 Gere uma chave segura no terminal:
#    python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=sua_chave_jwt_super_segura_com_32_caracteres_no_minimo
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# -------- API --------
API_HOST=0.0.0.0
API_PORT=8000

# -------- CORS (Frontend) --------
# ⚠️  Separar múltiplas URLs por vírgula (SEM ESPAÇOS)
CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://127.0.0.1:8080

# -------- LOGGING --------
LOG_LEVEL=INFO
# Opções: DEBUG, INFO, WARNING, ERROR, CRITICAL

# ====================================================
# 📋 INSTRUÇÕES DE SETUP:
# ====================================================
# 1. Copie este arquivo para .env:
#    cp .env.example .env
#
# 2. EDITE .env com suas credenciais REAIS:
#    - DB_PASSWORD: sua senha MySQL real
#    - SECRET_KEY: rode no terminal:
#      python -c "import secrets; print(secrets.token_urlsafe(32))"
#    - CORS_ORIGINS: URLs do seu frontend (separar por vírgula)
#
# 3. ⚠️  NUNCA commite .env no Git
#    Verifique .gitignore contém: *.env
#    git status não deve listar .env
#
# 4. Em PRODUÇÃO:
#    ✅ Use senhas fortes (20+ caracteres, mix chars)
#    ✅ Mude SECRET_KEY a cada deploy
#    ✅ Reduza ACCESS_TOKEN_EXPIRE_MINUTES (ex: 15)
#    ✅ Use variáveis de ambiente do servidor
#    ✅ Considere AWS Secrets Manager, HashiCorp Vault
#    ✅ HTTPS obrigatório
#    ✅ Rate limiting ativado
#
# ====================================================
